import { ResponsesInterceptor } from './responses.interceptor';

describe('ResponsesInterceptor', () => {
  it('should be defined', () => {
    expect(new ResponsesInterceptor()).toBeDefined();
  });
});
