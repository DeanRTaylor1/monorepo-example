import { ExceptionsFilterFilter } from './exceptions-filter.filter';

describe('ExceptionsFilterFilter', () => {
  it('should be defined', () => {
    expect(new ExceptionsFilterFilter()).toBeDefined();
  });
});
